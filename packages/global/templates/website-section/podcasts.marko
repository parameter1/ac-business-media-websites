$ const { id, alias, name, pageNode } = input;

<global-website-section-feed-layout
  id=id
  alias=alias
  name=name
  page-node=pageNode
  feed-flow={ displayImage: true }
  node={ withDates: true, withTeaser: true }
  with-native-x=false
/>
